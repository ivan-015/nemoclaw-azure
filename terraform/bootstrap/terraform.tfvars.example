# Bootstrap-stage variables — copy to terraform.tfvars and fill in.
# DO NOT commit terraform.tfvars (gitignored at repo root).

# Your personal Azure subscription GUID. Must NOT be a production sub.
subscription_id = "00000000-0000-0000-0000-000000000000"

# Azure region for the state backend. Use the same region as the
# main deploy for in-region latency on `terraform plan`/`apply`.
# Allowed values: eastus, eastus2, centralus, westus2, westus3,
# northeurope, westeurope.
location = "centralus"

# Owner tag — your email address or GitHub handle.
owner = "you@example.com"

# cost-center tag value. Default 'personal' for personal-sub deploys;
# set to your team/cost-center name when deploying into a work or
# shared subscription.
# cost_center = "personal"

# Single shared resource group name. Both stages (bootstrap +
# root) deploy here. Default is fine for most deploys; only override
# if a naming policy mandates a prefix.
# resource_group_name = "rg-nemoclaw"

# Your laptop's current public egress IP as a /32 CIDR. Find it with:
#   curl -s https://api.ipify.org
# This becomes the only public IP allowed to reach the state storage
# account's blob endpoint. Update and re-apply when your IP changes
# (different network, new ISP, mobile hotspot, etc.). If you get
# locked out, recover via Azure portal or:
#   az storage account network-rule add \
#     --resource-group rg-nemoclaw \
#     --account-name <storage-account-name> \
#     --ip-address <your-new-ip>
operator_ip_cidr = "203.0.113.42/32"
